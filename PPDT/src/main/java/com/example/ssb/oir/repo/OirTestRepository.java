package com.example.ssb.oir.repo;

import com.example.ssb.oir.model.OirTest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OirTestRepository extends JpaRepository<OirTest, Long> {
}

