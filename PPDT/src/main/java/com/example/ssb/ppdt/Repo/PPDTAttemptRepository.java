package com.example.ssb.ppdt.Repo;

import com.example.ssb.ppdt.Entity.PPDTAttempt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PPDTAttemptRepository extends JpaRepository<PPDTAttempt,Long> {
}
